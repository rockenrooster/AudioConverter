# Third-party notices

This application uses FFmpeg shared libraries through `FFmpeg.AutoGen`.

Bundled FFmpeg/runtime components may include LGPL and GPL-licensed software depending on the selected FFmpeg build options.

Release artifacts must include the exact FFmpeg build configuration, license texts, and source availability instructions.
