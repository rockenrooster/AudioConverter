# FFmpeg source offer

For releases that ship FFmpeg DLLs, provide the corresponding FFmpeg source.

Include one of:

- The exact source archive used to build the DLLs.
- A durable source location plus the exact version/commit and patch set.

Also include:

- Configure command.
- Build scripts or reproducible build notes.
- SHA-256 hashes for shipped binaries.
